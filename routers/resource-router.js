const express = require('express');

const Recipes = require('../models/resource-model');

const router = express.Router();

//include endpoints for adding resources and for retrieving them.

module.exports = router;