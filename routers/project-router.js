const express = require('express');

const Recipes = require('../models/project-model');

const router = express.Router();

//include endpoints for adding projects and for retrieving them.

module.exports = router;